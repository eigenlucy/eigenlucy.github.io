<!-- DEBUG FIGURE.LIQUID (SIMPLIFIED): include.path is [{{ include.path }}] -->
{% if include.path and include.path != "" %}
  <figure>
    <img 
      src="{{ include.path | relative_url }}" 
      alt="{% if include.alt %}{{ include.alt }}{% else %}Gallery Image{% endif %}" 
      style="width: 100%; height: auto; border: 1px solid red;" {% comment %} Added border for visibility {% endcomment %}
      {% if include.class %} class="{{ include.class }}" {% endif %}
      {% comment %} We will use eager loading for this test to ensure it tries to load {% endcomment %}
      loading="eager"
    />
    {% if include.caption %}
      <figcaption class="caption">{{ include.caption }}</figcaption>
    {% endif %}
  </figure>
{% else %}
<!-- DEBUG FIGURE.LIQUID (SIMPLIFIED): include.path was empty or nil. Path expected. -->
{% endif %}
